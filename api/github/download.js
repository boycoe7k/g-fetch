export default async function handler(req,res){const{repo,tag,asset}=req.query;if(!repo||!/^[^/]+\/[^/]+$/.test(repo)||!tag||!asset)return res.status(400).json({error:"Missing repo, tag or asset"});const h={Accept:"application/vnd.github+json","User-Agent":"GHFetch/1.1"};if(process.env.GITHUB_TOKEN)h.Authorization=`Bearer ${process.env.GITHUB_TOKEN}`;try{const r=await fetch(`https://api.github.com/repos/${repo}/releases/tags/${encodeURIComponent(tag)}`,{headers:h});const d=await r.json();if(!r.ok)return res.status(r.status).json({error:d.message||"Release not found"});const a=d.assets?.find(x=>x.name===asset);if(!a)return res.status(404).json({error:"Asset not found"});const f=await fetch(a.browser_download_url,{redirect:"follow"});if(!f.ok||!f.body)return res.status(f.status||502).json({error:"Asset download failed"});res.setHeader("Content-Type",f.headers.get("content-type")||"application/octet-stream");res.setHeader("Content-Disposition",`attachment; filename="${a.name.replaceAll('"','')}"`);return new Response(f.body,{status:200,headers:{"Content-Type":f.headers.get("content-type")||"application/octet-stream","Content-Disposition":`attachment; filename="${a.name.replaceAll('"','')}"`}}).arrayBuffer().then(b=>res.send(Buffer.from(b))).catch(()=>res.status(502).json({error:"Asset download failed"}))}catch{return res.status(502).json({error:"Unable to download asset"})}}
